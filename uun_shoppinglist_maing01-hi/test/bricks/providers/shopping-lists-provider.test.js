import UunShoppinglist from "uun_shoppinglist_maing01-hi";
import { mount, wait } from "uu5g05-test";

describe(`UunShoppinglist.Bricks.Providers.ShoppingListsProvider`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<UunShoppinglist.Bricks.Providers.ShoppingListsProvider onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
